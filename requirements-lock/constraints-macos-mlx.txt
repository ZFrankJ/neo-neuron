# macOS Apple Silicon MLX/PyTorch parity constraints.
#
# Verified on 2026-06-30 with Python 3.11.15 on arm64 macOS.
# The MLX packages stay pinned because MLX 0.30+ showed numerical drift against
# the current strict reference thresholds. General Python packages are bounded
# instead of exact-pinned so minor/patch resolver differences do not block local
# validation unnecessarily.

torch>=2.12,<2.13
numpy>=2.4,<2.5
datasets>=5.0,<6
transformers>=5.12,<6
PyYAML>=6.0,<7
psutil>=7.2,<8
matplotlib>=3.11,<4
thop==0.1.1.post2209072238
pytest>=9.1,<10

mlx==0.29.4; platform_system=="Darwin" and platform_machine=="arm64"
mlx-metal==0.29.4; platform_system=="Darwin" and platform_machine=="arm64"
